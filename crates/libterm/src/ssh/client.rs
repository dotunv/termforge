// SSH client — Phase 2 implementation using russh.
// Placeholder keeps the module tree compilable during Phase 1.
pub struct SshClient;

impl SshClient {
    pub fn new() -> Self { Self }
}

impl Default for SshClient {
    fn default() -> Self { Self::new() }
}
