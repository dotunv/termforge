// Phase 3: colour-coded tab bar (green/blue/purple dots).
pub struct TabBar;
impl TabBar {
    pub fn new() -> Self {
        Self
    }
}
impl Default for TabBar {
    fn default() -> Self {
        Self::new()
    }
}
